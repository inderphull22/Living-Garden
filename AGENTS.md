# AGENTS.md

## Cursor Cloud specific instructions

This is an empty/freshly initialized repository ("Living-Garden") with a single `README.md`. There are no source files, dependencies, build systems, services, tests, or linting configurations as of the initial commit.

- **No package manager lockfile or dependency manifest exists** (no `package.json`, `requirements.txt`, `Cargo.toml`, `go.mod`, etc.).
- **No dev server, build, lint, or test commands are available.**
- **No Docker or container configuration is present.**

When source code and a tech stack are added, this file should be updated with:
- How to install dependencies
- How to run lint, tests, build, and dev server
- Any non-obvious startup caveats or environment requirements
